defmodule Geometery do
	def rectangle_area(a,b) do 
		a * b 
	end 
	def square_area do
		rectangle_area(a, a)
	end
end 
defmodule Rectangle do
	def area(a), do: area(a,a)
	
	def area(a,b, do: a * b
	end
end